## Это краткая шпаргалка по BASH и утилитам

***

Команды бывают встроенными в оболочку или отдельными утилитами. 
Наборы утилит для разных компьютеров могут различаться.
Здесь в основном приведены утилиты из стандартного набора, работающего в большинстве случаев.

***
### Помощь в понимании, что именно делает команда

```help``` - Встроенная. Выведет список доступных встроенных команд. Не работает для утилит.

```help <команда>``` - Встроенная. Выведет краткую справку для конкретной встроенной команды. Не работает для утилит.

```<команда/утилита> --help``` - Альтернативный вариант краткой справки. Работает  для команд и утилит.

```type <команда>``` - Встроенная. Выведет путь к используемой утилите, или сообщение о том, что это встроенная команда или псевдоним.

```type -a <команда/утилита>``` - Встроенная. То же самое, но выведет список всех возможных для использования вариантов. 

```man <утилита>``` - Утилита. Расширенная справка для утилиты. Не работает для встроенных команд. 

### Напоминалка по операциям и клавишам
 
 Кнопки &uarr; | &darr; - Перемещение по истории команд. Начни вводить команду и нажми &uarr;  Если повезёт, то увидишь эту же предыдущую команду
 
 ```history``` - Вывод всей сохранённой истории команд

```clear``` - очистка экрана (прокрутка)

```reset``` - Полная очистка экрана

```Клавиша Tab``` (&harr;) - контекстное дополнение команды, имени переменной, файла или пути. Начни что-то вводить и нажми TAB

```./file_name``` - Запуск на выполнение какого-либо исполняемого файла. Подробнее см. [Исполнение файлов](#file_execute)

```|``` - Pipe, конвейер. Основная мощь BASH. Вместо вывода на экран результа передаётся следующей команде. 

``` > ``` - перенаправление вывода (результата работы команды/утилиты) в файл. Файл будет перезаписан.

``` >> ``` - то же самое, но предыдущие данные будут сохранены, новые будут дописаны в конец файла.

### Навигация по файлам и каталогам

<details>
  <summary> 👉 cd - Встроенная. Перейти в каталог по указанному пути. </summary>
  
  ```cd <путь>``` -  Путь может быть относительным (от текущего каталога) или абсолютным (относительно корня /)

  ```cd ~``` - Перейти в домашний каталог пользователя
  ```cd -``` - Вернуться в предыдущий каталог
</details>

```pwd``` - Встроенная. Отобразить путь к текущему каталогу от корня файловой системы.
  

<details>
  <summary>👉 ls - Утилита. Показать содержимое текущего каталога.</summary>
  
  ```ls -a``` - показать содержимое ткущего каталога, включая скрытые файлы (Их имена начинаются с . )
  
  ```ls -la``` - вывод содержимого в расширенном формате
  
  ```ls -lah``` - вывод содержимого в расширенном формате с человекопонятным размером файлов
</details>

<details>
  
  <summary>👉 Just Anime girl</summary>
  
  ![](Anime-pseudo.jpeg)

</details>

### Работа с файлами и каталогами

<details> 
  <summary>👉 mkdir - Утилита. Создать каталог</summary>
  
  ```mkdir <имя каталога>``` - создать в текущем месте каталог с указанным именем
  
  ```mkdir -p <полный путь>``` - создать по указанному пути каталог, включая все отсутствующие родительские
  
  ```mkdir <имя каталога>{последовательность}``` - Создать несколько каталогов. 
  
  Пример. Команда mkdir mycat-{a,b,c} создаст 3 каталога: mycat-1, mycat-2, mycat-3
  Последовательность может быть буквенная. Допустимо использовать синтаксис {1..100} {a..z}. Будут подставлены значения от 1 до 100 и от a до z.
  
</details>

```touch <имя файла>``` - Утилита. Создать пустой файл. Можно создать несколько файлов см.   ```mkdir```

```cp <что> <куда>``` - Утилита. Скопировать файл, каталог целиком. 

```mv <что> <куда>``` - Утилита. Переместить файл, каталог целиком.
<details>
 <summary>👉 cat - Утилита. Вывод текста.</summary>
 
 ```cat <имя файла>``` - выведет содержимое файла на экран.
 
 ```cat <имя файла1, имя файла2...>```  - выведет содержимое нескольких файлов.

```cat << EOF > <имя файла>``` - Неочевидный способ использования утилиты ```cat``` для многострочного ввода с клавиатуры в файл. 
Здесь EOF - слово-маркер, означающий конец ввода. Операция ```>``` - запись в файл с обнулением предыдущих данных.
Обычно после этой команды вводят построчноданные, а последняя строка должна быть ```EOF```
```
Line 1
My line 2
....
Line 100500
EOF
```
</details>

<details>
  <summary>👉 chmod - Утилита. Сменить атрибуты файла<a name="file_attribs"></a></summary>
  
 ```chmod 755 <имя файла>``` - устанавливает атрибуты файла в состояние rwxr-xr-x
 
 ```chmod +x <имя файла>``` - добавляет атрибут x ко всем правам

 ```chmod -R 755 <имя каталога>``` - Изменяет рекурсивно права для всех файлов и подкаталогов внутри каталога с указанным именем.
 
 
 ```ls -la``` - команда выведет список файлов в виде таблицы. 
  
  В первой колонке таблицы находятся атрибуты файла или каталога.
  Атрибуты каталога начинаются с буквы d. Атрибуты простого файла начинаются с -.
  Буквы rwx обозначают доступность файла: r(ead) - для чтения w(rite) - для записи e(x)ecute - для исполнения
  Первая тройка букв rwx относится к владельцу файла. (Это вторая колонка таблицы)
  Вторая тройка - к группе владельцев (Это третья колонка таблицы)
  Третья тройка - ко всем остальным.
  Таким образом, права rwxr-xr-- можно прочитать так: 
  Владелец имеет право на чтение, запись и исполнение. Группа может читать и исполнять. Остальные только читать.

  Что означают цифры 755 и как они соотносятся с буквами rwxr-xr-x. 
  7 - rwx
  5 - r-x 
  5 - r-x
  В двоичном представлении 7 = 111 5 = 101. Такой способ кодирования информации называется битовые флаги.
  В данном случае битовых флагов 9 шт, по одному на каждую букву атрибута. Бит, установленный в 1 означает, что атрибут включен.

  Ещё пример: ```chmod 611 my_file.sh``` Права на my_file.sh будут выглядеть как rw---x--x. 6 = 110 = (rw-) 1 = 001 (--x)
  
</details>

<details>
  <summary>👉 <a name="file_execute"></a>Исполнение файлов </summary>
 
  ```./<имя файла>``` - Чтобы выполнить файл (запустить программу) необходимо перейти в каталог с файлом и набрать эту команду. 
  
  Чтобы в Linux файл был выполняемым, должны быть соблюдены два условия:
 
 + В атрибутах файл должен быть установлен флаг исполнения (х). Подробнее см. [chmod](#file_attribs)
 + Файл должен быть или скомпилированной программой или текстовым скриптом. 
 
 Первая строка текстового скрипта должна указывать на командный процессор,  который будет выполнять дальнейшие инструкции. 
 Эта строка называется ```shebang```.
 В случае с BASH она выглядит примерно так: ```!# /usr/bin/env bash``` В зависимости от операционной системы возможны вариации.
 
 ❗Важно. Эта строка должна быть первой. Пробелов в начале строки быть не должно. После первой строки желательно оставлять одну пустую строку.
 
</details>




### Работа со строками
❗BASH любое значение интерпретирует как строку. Вычисления и обработку чисел см. [ТУТ](#вычисления)



### Вычисления
